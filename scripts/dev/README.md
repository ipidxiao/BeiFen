# Dev-only scripts

Ad-hoc probes and crawlers — not part of `npm test` or release build.

| File | Purpose |
|------|---------|
| `_probe_pdf.mjs` | PDF.js load probe |
| `scratch_itch_urls.mjs` | itch.io URL scratch |
| `crawl_progress.json` | Crawl state snapshot |
